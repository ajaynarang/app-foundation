/** Minimum trimmed query length before a Places autocomplete request fires. */
export const PLACES_MIN_QUERY_LENGTH = 3;

/** Debounce window for as-you-type Places queries. */
export const PLACES_DEBOUNCE_MS = 200;

/** Max suggestions requested per autocomplete call. */
export const PLACES_SUGGESTION_LIMIT = 5;
