'use client';

/**
 * Weather overlay — Phase 3+ stub. Renders nothing today; included so the
 * map composition is final-shaped and Phase 3 only needs to fill this in.
 */
export function WeatherOverlay() {
  return null;
}
