'use client';

import { AiSpendPage } from '@/features/admin/ai-spend';

/**
 * Super-admin AI spend dashboard — per-tenant AI cost, budgets, and
 * invocation history.
 */
export default function AdminAiSpendPage() {
  return <AiSpendPage />;
}
