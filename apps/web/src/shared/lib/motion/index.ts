// Platform Motion System — Unified Animation Architecture
// Import from '@/shared/lib/motion' for all animation needs.

// Constants & presets
export { duration, easing, spring, stagger, transition, distance } from './config';

// Reusable Framer Motion variants
export {
  fadeIn,
  slideUp,
  slideDown,
  scaleIn,
  pageEnter,
  staggerContainer,
  staggerItem,
  tableRowVariant,
  tableContainer,
} from './variants';

// Ready-to-use animated components
export { PageTransition, FadeIn, SlideUp, ScaleIn, AnimatedList, AnimatedListItem } from './components';
