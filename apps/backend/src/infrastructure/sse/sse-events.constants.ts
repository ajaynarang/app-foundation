/**
 * Re-export from shared-types — single source of truth for SSE event constants.
 * Both backend and frontend import from @app/shared-types.
 */
export { SSE_EVENTS, type SseEventType } from '@app/shared-types';
