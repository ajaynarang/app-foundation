/**
 * Re-export from shared-types — single source of truth for SSE event constants.
 * Both backend and frontend import from @sally/shared-types.
 */
export { SSE_EVENTS, type SseEventType } from '@sally/shared-types';
